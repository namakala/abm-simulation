# TODO — Quarto Demos Refactor

## WP-1: Create `_quarto.yml` for demos directory

- [x] Write test: `_quarto.yml` exists and has valid YAML structure with expected fields
- [x] Create `src/python/demos/_quarto.yml` with correct configuration

## WP-2: Create `index.qmd` for demos directory

- [x] Write test: `index.qmd` exists, is valid YAML+QMD, references all 14 demo pages
- [x] Create `src/python/demos/index.qmd` matching the current `index.html` structure

## WP-3: Fix `PROJECT_ROOT` in all 14 `.qmd` files

- [x] Write test: no `.qmd` file contains `PROJECT_ROOT` variable
- [x] Write test: all `.qmd` files have `demos_dir`, `project_root`, and `sys.path.insert` in setup block
- [x] Fix all 14 demo `.qmd` files

## WP-4: Update `pixi.toml` tasks and dependencies

- [x] Write test: `pixi.toml` has `quarto` task pointing to `src/shell/quarto-render.sh`
- [x] Write test: `pixi.toml` has `serve` task using `quarto preview`
- [x] Write test: `pixi.toml` has no `serve-quick` task
- [x] Update `quarto` task in `pixi.toml` to use the render script
- [x] Update `serve` task in `pixi.toml` to use `quarto preview`

## WP-5: Update `.gitignore`

- [x] Write test: `.gitignore` contains `src/python/demos/*.html`
- [x] Write test: `.gitignore` contains `src/python/demos/_site/`
- [x] Add HTML outputs and `_site/` to `.gitignore`

## WP-6: Delete `src/shell/serve.sh`

- [x] Write test: `src/shell/serve.sh` does not exist
- [x] Delete `src/shell/serve.sh`

## WP-7: End-to-end verification

- [x] Run `pixi install` successfully
- [x] Run `bash src/shell/quarto-render.sh` and verify demos render
- [x] Verify `index.html` exists in `_site/`
- [x] Verify `serve` task starts `quarto preview`

## Known Issues

- `agent_initialization_demo.qmd` has a pre-existing bug: uses `config["coping_success_rate"]` which no longer exists in the codebase. This is a demo-level bug, not a refactoring issue. Demo renders correctly with the old `.html` but fails with the new rendering.
- `index.html` (plain HTML) was deleted from `.gitignore` since it is now generated by `index.qmd` via `_quarto.yml`
